pre-commit:
  parallel: true
  commands:
    lint:
      glob: '*.{ts,tsx}'
      run: npx eslint {staged_files}
    format:
      glob: '*.{ts,tsx,json,md}'
      run: npx prettier --check {staged_files}
    typecheck:
      glob: '*.{ts,tsx}'
      run: pnpm --filter @zpan/server typecheck
